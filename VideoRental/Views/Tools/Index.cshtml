@model IEnumerable<InventoryRental.Models.Tool>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
    <a href="@Url.Action("Create New", "Tools")" class="btn btn-default"><i class="fa fa-plus"></i> Create New</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new {id = item.ToolId})|
        @Html.ActionLink("Details", "Details", new { id = item.ToolId }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.ToolId })*@

                <a href="@Url.Action("Edit", "Tools", new {id = item.ToolId})" class="btn btn-default"><i class="fa fa-pencil"></i> Edit</a>
                <a href="@Url.Action("Details", "Tools", new {id = item.ToolId})" class="btn btn-default"><i class="fa fa-info"></i> Details</a>
                <a href="@Url.Action("Delete", "Tools", new {id = item.ToolId})" class="btn btn-default"><i class="fa fa-trash"></i> Delete</a>
            </td>
        </tr>
    }

</table>

@section scripts
{
    <script>
        $(function() {
            var successMessage = '@TempData["SuccessMessage"]'
            if (successMessage != '')
                alertify.success(successMessage);
        });
    </script>
}
